[package]
name = "mempool-mock"
version = "0.1.0"
edition = "2021"

[dependencies]
axum = { version = "0.7" }
hex = "0.4.3"
rand = "0.8.5"
serde = { version = "1", features = ["derive"] }
tokio = { version = "1", features = ["full", "tracing"] }
